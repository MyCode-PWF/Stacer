{
    "version": "2.0.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "gdb",
            "request": "launch",
            "target": "./build/output/stacer",
            "cwd": "${workspaceRoot}",
            "preLaunchTask": "build"
        }
    ]
}